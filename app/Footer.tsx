export default function Footer() {
  return (
    <footer className="mt-10 border-t border-line px-5 py-8">
      <div className="mx-auto flex max-w-[1080px] flex-col gap-2 text-[13px] text-ink-soft sm:flex-row sm:flex-wrap sm:justify-between sm:gap-3">
        <span>© Ibra — Market &amp; Growth Advisory</span>
        <span>Sessions scheduled after booking</span>
        <span>Prices shown in AED</span>
      </div>
    </footer>
  );
}
